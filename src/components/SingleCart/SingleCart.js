import React from 'react';
import './SingleCart.css'

const SingleCart = ({pd}) => {
    // console.log(pd)
    return (
        <div>
            
        </div>
    );
};

export default SingleCart;